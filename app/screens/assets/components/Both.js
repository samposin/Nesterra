import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Both = () => {
  return (
    <View>
      <Text>Both</Text>
    </View>
  );
};

export default Both;

const styles = StyleSheet.create({});
